#include <stdio.h>

/**
 * main - Program entry point.
 *
 * Return: Always 0 (success).
 */
int main(void)
{
	char *str2print = "\"Programming is like building a multilingual puzzle";

	puts(str2print);

	return (0);
}
